# StockScan Inventory Dashboard

## Overview
StockScan is a full-featured inventory management web portal that connects to an external NestJS backend at `https://api.stockscan.uk`. The frontend is built with Next.js 14 (App Router) and deployed at `https://login.stockscan.uk`.

## Technical Stack
- **Frontend**: Next.js 14, TypeScript, Tailwind CSS
- **Charts**: recharts (for reports)
- **API Client**: Custom `lib/api-client.ts` with token-based auth proxied through `/api/proxy/[...path]`
- **Auth**: Custom auth context with JWT tokens stored in cookies
- **Backend**: External NestJS API at `https://api.stockscan.uk` (NOT in this project)

## Key Architecture
- Backend returns `token` (not `access_token`) from login
- Job card endpoints use `/job-cards/` prefix (no `/api/` in proxy path)
- Allocations endpoint returns array directly, not wrapped object
- **Delivery system (v1.14.1)**: Unified delivery system handling CUSTOMER deliveries and TRANSFER delivery notes. DeliveryType: CUSTOMER | TRANSFER. FulfillmentMethod: DELIVERY | COLLECTION. Status: PENDING | DELIVERED | COLLECTED | CANCELLED. Signature via `/api/deliveries/:id/signature` (auto-sets status based on fulfillmentMethod). POS uses `createDeliveryFromSale(saleId)`. Stock transfers use `createDeliveryFromTransfer(transferId)`. PDF auto-adapts. Standalone stock transfer detail page removed (redirects to /deliveries). Delivery creation requires `productId: null`, `sku`, `productName` format for Sage imports

## Modules Implemented (v1.7.4)
### Phase 1 - Job Cards Enhancements
- **4-tab layout**: Materials, Labour, Activity, Notes
- **Status workflow**: OPEN → IN_PROGRESS → COMPLETED → CLOSED with dynamic action button
- **Invoice generation**: via `GET /job-cards/:id/invoice`
- **Sage CSV export**: via `GET /job-cards/:id/export-sage`
- **Activity log**: Timeline UI from `GET /job-cards/:id/activity`
- **Notes**: Editable textarea, PUT `/job-cards/:id/notes`

### Phase 2 - Reports Module (`/reports`)
- Sales, Stock, Job Cards, Profit & Loss tabs
- Date range filtering, CSV export
- recharts charts (Line, Bar, Pie)
- P&L tab restricted to OWNER role

### Phase 3 - POS Module (`/pos`)
- New Sale view: product search, cart, payment method selection, checkout
- Sales History view: table with sale details, refund capability
- Receipt generation in popup window
- Payment methods: Cash, Card, Transfer

### Phase 4 - Branding & Feature Flags (`/branding`)
- Company branding: name, logo URL, primary/secondary colors with preview
- Feature flags toggle: all 8 feature modules can be enabled/disabled
- OWNER-only access

## Navigation (Sidebar)
Dashboard, Products, Transactions, Job Cards, Deliveries, POS, Invoices, Warehouses, Stock Transfers, Stocktakes, Purchase Orders, Reports, Barcode Scanner, Suppliers, Import, Invitations, Users, Subscription, Mobile App, Company Profile, Branding, Settings

## New Pages (v1.7.5+)
- `/invoices` - Unified invoices page showing POS sales + job card invoices in one table
- `/settings` - Now has tabs: Currency + Integrations (Xero, QuickBooks, Sage)
- `/privacy` - GDPR-compliant Privacy Policy (public, no auth)
- `/terms` - EULA / Terms of Service (public, no auth)
- Legal links shown on login page footer
- `/warehouses` - Multi-location warehouse management (MANAGER/OWNER)
- `/stock-transfers` - Transfer stock between warehouses (MANAGER/OWNER)
- `/stocktakes` - Physical stock counting and reconciliation (MANAGER/OWNER)
- `/purchase-orders` - Purchase order management with supplier integration (MANAGER/OWNER)
- `/company-profile` - Business details, VAT settings, receipt configuration with live preview (MANAGER/OWNER)
- `/barcode-scanner` - Camera-based barcode scanning (html5-qrcode), manual lookup, product code management (ALL roles)

## Reports Enhancement
- Reports page now has 5 sections: Stock, Financial, Transaction, Sales, Adjustment
- Sales section (MANAGER/OWNER): Summary, By Product, By Method sub-tabs
- API methods: getReportSalesSummary, getReportSalesByProduct, getReportSalesByMethod

## Accounting Integrations
- Settings → Integrations tab shows Xero, QuickBooks, Sage with connect/disconnect buttons
- Uses `/api/proxy/accounting/{platform}/status` and `/connect` endpoints
- Gracefully handles 404 when backend endpoints aren't available yet
- Logos stored at `/public/xero-logo.png`, `/quickbooks-logo.png`, `/sage-logo.png`

## Download / Install App Page (`/download`)
- Browser & OS auto-detection with install guidance
- One-click PWA install button (when supported)
- Edge instructions for Windows users on non-Chromium browsers
- Comparison table: Web vs PWA vs Desktop App
- "Coming Soon" placeholder for Windows .exe download
- Linked in sidebar as "Install App"

## Electron Desktop App Scaffold (`/electron-app/`)
- Located at `/home/ubuntu/inventory_dashboard/electron-app/` (outside nextjs_space)
- Downloadable zip at `/public/downloads/stockscan-desktop-scaffold.zip`
- **main.js**: Window management, system tray, app menu, auto-updater
- **wizard.html**: First-run setup wizard (SaaS vs Enterprise server selector with test connection)
- **preload.js**: Secure IPC bridge for wizard communication
- **package.json**: electron-builder config for Windows NSIS, macOS DMG, Linux AppImage
- **assets/**: icon.ico (Windows), icon.png (Linux) generated from StockScan logo
- Injects server config into localStorage on portal load — seamless with ServerConnectionContext
- README.md with full build instructions, CI/CD GitHub Actions template

## PWA Support (Phase 1 - Desktop Roadmap)
- **Manifest**: `/public/manifest.json` — standalone display, slate-900 theme, shortcuts for Dashboard/POS/Products/Scanner
- **Service Worker**: `/public/sw.js` — network-first for pages, cache-first for static assets, skips `/api/` routes
- **Icons**: `/public/icons/` — 72-512px standard + maskable variants + Apple touch icon
- **PWA Register**: `components/pwa-register.tsx` — captures beforeinstallprompt, shows install banner, handles SW updates
- **Layout**: `manifest` in metadata, `theme-color` meta tag, Apple PWA meta tags, PWARegister component in body

## Multi-Server Connection (Standalone Mode)
- **ServerConnectionContext** (`contexts/server-connection-context.tsx`): Manages which backend API the portal connects to
- **Server types**: `saas` (default: `https://api.stockscan.uk`) or `enterprise` (custom URL)
- **Storage**: Backend URL stored in `localStorage` under keys `stockscan_server_url`, `stockscan_server_label`, `stockscan_server_type`
- **Proxy passthrough**: `api-client.ts` sends `x-backend-url` header → proxy route reads it dynamically
- **Settings → Server tab**: SaaS/Enterprise type selector, URL config, test connection, save/reset
- **Login page**: Shows server badge (Cloud vs Enterprise with label)
- **Sidebar**: Connection indicator at bottom showing current server
- **Auth cleared on server switch**: User must re-login when changing servers

## Test Accounts
- Owner (primary): `parts@ridgewayplant.co.uk` (current active test account)
- Owner (secondary): `parts@rlmiller.co.uk` / `RLMiller2026!`
- Owner: `owner@inventory.com` / `owner123`

## API Client Methods
All methods in `lib/api-client.ts` cover: products, transactions, deliveries, job cards (full CRUD + allocations, labour, activity, notes, invoice, sage export), reports (sales, stock, job-cards, P&L), POS (sales, refunds, receipts), feature flags, company branding, warehouses (CRUD + stock levels), stock transfers, stocktakes, purchase orders (CRUD + receive).

## New Pages (v1.8.0 - Warehouse & Supply Chain)
- `/warehouses` - Multi-warehouse overview with charts + management (CRUD). Overview/Manage toggle views. Stock drill-down per warehouse.
- `/stock-transfers` - Transfer stock between warehouses with product search from source warehouse stock
- `/stocktakes` - Initiate and view stocktakes per warehouse with variance tracking
- `/purchase-orders` - Create and manage purchase orders with supplier selection, product search, pricing
- Dashboard now shows Warehouse Overview section for MANAGER/OWNER roles

## Component Notes
- `SelectInput` now supports both `options` prop AND `children` pattern (children takes precedence)
- `Badge` variants: default, success, warning, danger, info, outline
- `Button` variants: default, destructive, outline, secondary, ghost, link, success
- `DashboardLayout` uses `allowedRoles` prop (not `requiredRoles`)
